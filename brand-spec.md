# Brand Spec - Legendários

- **Background (`--bg`)**: `oklch(15% 0.01 0)`
- **Surface (`--surface`)**: `oklch(20% 0.01 0)`
- **Foreground (`--fg`)**: `oklch(98% 0 0)`
- **Muted (`--muted`)**: `oklch(70% 0 0)`
- **Border (`--border`)**: `oklch(30% 0 0)`
- **Accent (`--accent`)**: `oklch(60% 0.22 35)` (Laranja Vibrante)

- **Display Font**: `'Montserrat', sans-serif` (Black/900, uppercase)
- **Body Font**: `'Inter', sans-serif`

**Posture:**
- Dark mode nativo.
- Tipografia display estendida, em caixa alta, com tracking ajustado.
- Imagens com overlay escuro intenso.
- Apenas um accent (laranja) para guiar a atenção.
